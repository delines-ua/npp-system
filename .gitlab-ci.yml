stages:
  - build

build:
  stage: build
  tags:
    - shell
  script:
    - node --version
    - npm --version
    - npm ci
    - npm run build
  artifacts:
    paths:
      - dist/
    expire_in: 1 hour